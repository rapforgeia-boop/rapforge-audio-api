services:
  - type: web
    name: rapforge-audio-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: REPLICATE_API_TOKEN
        sync: false
